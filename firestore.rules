rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all read/write for development
    // TODO: Replace with secure rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
